[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tm4c-ota"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pyserial>=3.5"]

[project.optional-dependencies]
test = ["pytest>=8"]

[project.scripts]
tm4c-ota = "tm4c_ota.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
